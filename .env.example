# CLITOPHONE Configuration
# Copy this file to .env and customize

# ─────────────────────────────────────────────────────────
# Machine Identity
# ─────────────────────────────────────────────────────────

# Emoji or short identifier for this machine (shown in TMUX)
CLITOPHONE_MACHINE_ID="🖥️"

# Default TMUX session name
CLITOPHONE_DEFAULT_SESSION="dev"

# ─────────────────────────────────────────────────────────
# Claude Code Settings
# ─────────────────────────────────────────────────────────

# Model to use (comment out for default)
# CLAUDE_MODEL="claude-sonnet-4-20250514"
# CLAUDE_MODEL="claude-opus-4-20250514"

# ─────────────────────────────────────────────────────────
# Credentials (macOS Keychain)
# ─────────────────────────────────────────────────────────

# Keychain service name for API key storage
KEYCHAIN_SERVICE="anthropic-api"

# To store your API key in macOS Keychain:
# security add-generic-password -s "anthropic-api" -a "$USER" -w "sk-ant-..."

# ─────────────────────────────────────────────────────────
# SSH Settings
# ─────────────────────────────────────────────────────────

# Path to SSH key (for agent unlocking)
SSH_KEY_PATH="$HOME/.ssh/id_ed25519"

# ─────────────────────────────────────────────────────────
# Linux GPG Encryption (optional)
# ─────────────────────────────────────────────────────────

# If using GPG-encrypted API key on Linux:
# 1. Create encrypted file:
#    echo "sk-ant-..." | gpg -e -r your@email.com > ~/.config/anthropic/api_key.gpg
# 2. unlock.sh will automatically decrypt it
